import React, { Component } from "react";

class UserCart extends Component{
    render(){
        return(
            <div>Where View for Guest USerCart will Go </div>
        );
    }




        // constructor(){
        //     super()

        //     const OriginalUserCart (req, res, next) {
        //         req.body = [], // defualts to empty, no drops of stored memory at all in OG-User-Cart (this will send it's empty state to a HOC element)
        //         req.params: "Cart Empty"
        //         res.body(404).send()
        //     };
        // };
};

export default UserCart;